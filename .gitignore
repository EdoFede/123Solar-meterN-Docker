# Environment files (never commit real secrets/config)
.env

# Local build artifacts
build_tmp/
*.log

# App code + config/data live in Docker volumes (populated at first boot by
# php/install-apps.sh), never in the repo.

# OS / editor cruft
.DS_Store
*.swp
