; php/conf.d/zz-app.ini — php.ini overrides (SCAFFOLD)
;
; To be implemented. Expected overrides:
;   - error logging to stderr (visible via `docker logs`)
;   - timezone, memory_limit, etc. as required by the apps
;
; The "zz-" prefix ensures it is loaded last.
