# php/Dockerfile — UNICA immagine custom, multi-stage (SCAFFOLD)
#
# Da implementare. Struttura prevista:
#   Stage `builder`: compila i tool C (libmodbus, sdm120c, aurora)
#   Stage runtime:   FROM php:8.x-fpm-alpine
#                    apk add rrdtool; copia binari dal builder;
#                    gruppi dialout/uucp per accesso seriale; entrypoint.sh
#
# Vedi docs/GUIDA-IMPLEMENTAZIONE.md §3.2 e docs/ANALISI-PROGETTO.md §4.
