; php/fpm/www.conf — pool php-fpm (SCAFFOLD)
;
; Da implementare. Configurazione prevista:
;   - listen = 9000            (raggiungibile da nginx via fastcgi_pass php:9000)
;   - log di access/error del pool su stderr
;   - tuning pm.* secondo il carico
;
; Vedi docs/GUIDA-IMPLEMENTAZIONE.md §3.1.
