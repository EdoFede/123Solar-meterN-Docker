; php/fpm/www.conf — php-fpm pool (SCAFFOLD)
;
; To be implemented. Expected configuration:
;   - listen = 9000            (reachable from nginx via fastcgi_pass php:9000)
;   - pool access/error logs to stderr
;   - pm.* tuning according to load
