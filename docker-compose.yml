# docker-compose.yml — multi-container orchestration (SCAFFOLD)
#
# To be implemented in the implementation chat. Expected content:
#   - `web` service: official nginx:alpine, mounts nginx/default.conf + app-code
#     volume, exposes ${HTTP_PORT}
#   - `php` service: builds ./php, mounts the app-code volume at /var/www,
#     `devices: ${SERIAL_DEVICE}`, `env_file: .env`
#   - volumes: app-code (shared web<->php), 123solar config/data, metern config/data
#   - (optional) cron service for scheduled polling
