# nginx/default.conf — server block (SCAFFOLD)
#
# Da implementare. Contenuto previsto:
#   - root /var/www
#   - routing /123solar e /metern
#   - basic-auth su /admin
#   - fastcgi_pass php:9000 per gli script PHP
#
# Montato nel container nginx ufficiale (non serve immagine custom).
# Vedi docs/GUIDA-IMPLEMENTAZIONE.md §3.2.
