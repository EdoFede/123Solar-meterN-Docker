# nginx/default.conf — server block (SCAFFOLD)
#
# To be implemented. Expected content:
#   - root /var/www
#   - routing for /123solar and /metern
#   - basic-auth on /admin
#   - fastcgi_pass php:9000 for PHP scripts
#
# Mounted into the official nginx container (no custom image needed).
